DIAGRAM: Hybrid and Multi-Cloud Stack
PURPOSE: Shows the recommended hybrid/multi-cloud connectivity stack in three tiers: Client communication (Verified Access, Client VPN), Multi-cloud (AWS Interconnect - multicloud, Partner-based), and Hybrid connectivity (Direct Connect, VPN, SD-WAN). Represents the building-your-stack view.
LAYOUT: top-to-bottom (three stacked tiers)
WIDTH: 700px maximum readable width

TITLE:
- Text heading above the tiers, label "Hybrid & Multi-Cloud Connectivity Stack"

GROUPS:
- Client communication: top tier; border style dashed, color green (#059669)
- Multi-cloud: middle tier; border style dashed, color purple (#7c3aed)
- Hybrid connectivity: bottom tier; border style dashed, color blue (#2563eb)

NODES:
Client communication:
- Verified Access: AWS icon "AWS Verified Access", fill green (#059669), label "AWS Verified Access\nZero-trust application access"
- Client VPN: AWS icon "AWS Client VPN", fill purple (#8C4FFF), label "AWS Client VPN\nNetwork-layer user access"

Multi-cloud:
- Interconnect: AWS Interconnect product icon (embedded image, not a Direct Connect icon), label "AWS Interconnect - multicloud\nfor select CSP\n(preferred)"
- Partner: AWS icon "AWS Direct Connect", fill purple (#8C4FFF), label "Partner-based DX or VPN\nwhere AWS Interconnect unavailable"

Hybrid connectivity:
- Direct Connect: AWS icon "AWS Direct Connect", fill purple (#8C4FFF), label "AWS Direct Connect\nPrimary private path"
- VPN: AWS icon "AWS Site-to-Site VPN", fill purple (#8C4FFF), label "AWS Site-to-Site VPN\nFast-start or IPsec over DX\nwhen MACsec unavailable"
- SD-WAN: AWS icon "AWS Transit Gateway", fill blue (#2563eb), label "SD-WAN Connect attachments\nfor existing SD-WAN overlays"

CONNECTIONS:
- No connections (stack/tier reference diagram)

NOTES:
- BACKGROUND: Transparent (no white background). All text and lines that appear directly on the transparent background must use #7E7E7E instead of black (#000000). Text and lines inside colored nodes/shapes can remain as-is (white on colored fill is fine).
- Three tiers stacked vertically (green on top, purple middle, blue bottom)
- This mirrors the hierarchy from user-facing to infrastructure-level
- Each tier shows the recommended services for that concern
- Include brief context in labels about when to use each option
- All icons are 78x78 with labels below, centered
- The two-node tiers (Client communication, Multi-cloud) share the same two column positions so nodes align vertically across tiers; the three-node Hybrid tier is evenly distributed across the same width
- The Interconnect node uses the dedicated AWS Interconnect icon as an embedded image because the shape is not in the drawio AWS icon library; every other node uses a library AWS resource icon
